package se.flightplanner.map3d;

import java.util.HashMap;
import java.util.HashSet;

public class TexVertStore {
/*
	private class Usage
	{
		public TexVert texvert; 
		public int usage;
	}
	private HashMap<TexVert,Usage> store;
	
	TexVert obtain(Vertex v,short texu,short texv)
	{
		TexVert dummy=new TexVert(v,texu,texv);
		Usage u=store.get(dummy);
		if (u==null)
		{
			u=new Usage();
			u.texvert=dummy;
			u.usage=1;
			store.put(dummy,u);
			return dummy;
		}
		else
		{
			u.usage+=1;
			return u.texvert;
		}
	}
*/	
}
